# Dependências de PRODUÇÃO (além das do requirements.txt)
# Instale com: pip install -r requirements.txt -r requirements-prod.txt

# Driver PostgreSQL (necessário em produção; SQLite é usado em desenvolvimento)
psycopg2-binary==2.9.9

# Servidor WSGI de produção (substitui uvicorn standalone)
gunicorn==23.0.0
